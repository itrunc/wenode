define(function(require) {
	var dialog = require('MDialog');
	dialog.show();
	/*
	dialog.confirm({
		title: 'Warning',
		message: 'haha',
		btnOKLabel: '确定',
		btnCancelLabel: '取消',
		btnOKClass: 'blue lighten-4',
		callback: function(result) {
			console.log(result);
		}
	});
	*/
});