<nav>
<div class="nav-wrapper blue accent-1">
	<a href="{{brand.link}}" class="brand-logo">{{brand.label}}</a>
	<a href="#" data-activates="sr-collapse" class="button-collapse"><i class="mdi-navigation-menu"></i></a>
	<ul class="right">
		<li><a class="dropdown-button" href="#" data-activates="dropdown-user">{{user.label}}<i class="mdi-navigation-arrow-drop-down right"></i></a></li>
	</ul>
	<ul class="right hide-on-med-and-down">
		{{#each main}}
		<li><a href="{{link}}">{{label}}</a></li>
		{{/each}}
	</ul>
  <ul class="right hide-on-med-and-down">
    <li><a class="dropdown-button" href="#" data-activates="dropdown-func">{{func.label}}<i class="mdi-navigation-arrow-drop-down right"></i></a></li>
  </ul>
	<ul class="side-nav" id="sr-collapse">
		{{#each side}}
		<li><a href="{{link}}">{{label}}</a></li>
		{{/each}}
	</ul>
</div>
</nav>
<ul id="dropdown-user" class="dropdown-content">
	{{#each user.items}}
	<li><a href="{{link}}">{{label}}</a></li>
	{{/each}}
</ul>
<ul id="dropdown-func" class="dropdown-content">
	{{#each func.items}}
    <li><a href="{{link}}">{{label}}</a></li>
	{{/each}}
</ul>